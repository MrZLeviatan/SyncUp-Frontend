/**
 * @enum GeneroMusical
 * @description Enumeración que representa los géneros musicales definidos en el backend.
 *
 * This enum mirrors the values of the backend Java enum `GeneroMusical`
 * to ensure consistency between frontend and backend.
 */
export enum GeneroMusical {
  POP = 'POP',
  ROCK = 'ROCK',
  REGUETON = 'REGUETON',
  RAP = 'RAP',
  METAL = 'METAL',
  NU_METAL = 'NU_METAL',
  ELECTRONICA = 'ELECTRONICA',
  HEAVY_METAL = 'HEAVY_METAL',
  BLACK_METAL = 'BLACK_METAL',
}
